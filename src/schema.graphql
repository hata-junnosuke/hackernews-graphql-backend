# GraphQLスキーマを定義
type Query {
    info: String! # !でnullを返すことはできない
    feed: [Link]!
  }

  type Mutation {
    post(url: String!, description: String!): Link!
  }

  type Link {
    id: ID!
    description: String!
    url: String!
  }